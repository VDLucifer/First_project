extends layout
block content
    h1= title
    p Please select a file to upload: #{title}
    form(action="/upload", method="post")
        inpute(type="text", name="title")
        inpute(type="file", name="upload", multiple="multiple")
        //textarea(name="text" rows="1" cols="20")
        input(type="submit", value="Submit text")
    //button(class="btn", data-action="upl").Be Submit
